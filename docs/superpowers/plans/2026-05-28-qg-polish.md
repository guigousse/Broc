# QG Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Quatre améliorations ciblées du QG : météo dans la WeekTimeline (gated par la compétence Bulletin météo), réordonnement État des lieux / Gazette, info loyer sur la ligne Stockage, et bouton "Passer" pour avancer d'un jour.

**Architecture :** Modifications UI uniquement. `WeekTimeline` reçoit une prop optionnelle `meteoSemaine` et reste agnostique des compétences (la gating se fait dans la page QG). `QgEtatDesLieux` enrichit la meta de la ligne Stockage. `qg/page.tsx` réorganise les sections et ajoute un bouton "Passer" branché sur `avancerJour(1)` (logique déjà existante dans `GameContext` — gère restauration, refresh hebdo, loyer, huissier).

**Tech Stack :** Next.js 15 (app router), React 19, TypeScript, lucide-react (icônes), pas de framework CSS (styles inline avec variables CSS).

**Spec :** `docs/superpowers/specs/2026-05-28-qg-polish-design.md`

**Vérification :** Pas de tests automatisés dans ce projet — la validation se fait par `npx tsc --noEmit`, `npm run build`, et inspection visuelle dans le navigateur (`npm run dev` → `/qg`).

---

### Task 1 : WeekTimeline — prop météo et seconde rangée

**Files :**
- Modify : `src/components/WeekTimeline.tsx`

- [ ] **Step 1 : Ajouter l'import du type Meteo et de METEO_ICON**

En haut du fichier, après l'import existant de `indexJourSemaine, JOURS_SEMAINE` :

```tsx
import { METEO_ICON } from "@/data/meteos";
import type { Meteo } from "@/types/game";
```

- [ ] **Step 2 : Étendre l'interface des props**

Remplacer :

```tsx
interface WeekTimelineProps {
  jourActuel: number;
}
```

par :

```tsx
interface WeekTimelineProps {
  jourActuel: number;
  /** Si fournie, affiche l'icône météo correspondante sous chaque jour. */
  meteoSemaine?: Meteo[];
}
```

- [ ] **Step 3 : Mettre à jour le rendu de chaque cellule**

Remplacer la fonction `WeekTimeline` entière par :

```tsx
export function WeekTimeline({ jourActuel, meteoSemaine }: WeekTimelineProps) {
  const idx = indexJourSemaine(jourActuel);
  return (
    <div
      role="list"
      aria-label="Semaine en cours"
      style={{ display: "grid", gridTemplateColumns: "repeat(7, 1fr)", gap: 3 }}
    >
      {labels.map((l, i) => {
        const isToday = i === idx;
        const isWeekend = i >= 5;
        const meteo = meteoSemaine?.[i];
        const Icon = meteo ? METEO_ICON[meteo] : null;
        const baseColor = isToday
          ? "var(--brass-300)"
          : isWeekend
            ? "var(--ink-700)"
            : "var(--ink-500)";
        const style: CSSProperties = isToday
          ? {
              ...cellBase,
              background: "var(--forest-800)",
              color: "var(--brass-300)",
              borderColor: "var(--brass-500)",
            }
          : isWeekend
            ? { ...cellBase, background: "var(--paper-200)" }
            : cellBase;
        return (
          <div
            key={i}
            role="listitem"
            style={{
              ...style,
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              gap: 2,
            }}
            title={JOURS_SEMAINE[i]}
          >
            <span>{l}</span>
            {Icon ? (
              <Icon size={12} strokeWidth={1.5} color={baseColor} aria-hidden />
            ) : null}
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4 : Vérifier le typecheck**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 5 : Commit**

```bash
git add src/components/WeekTimeline.tsx
git commit -m "feat(qg): WeekTimeline accepte une prop meteoSemaine et affiche les icônes"
```

---

### Task 2 : QgEtatDesLieux — ligne Stockage avec capacité + loyer

**Files :**
- Modify : `src/components/mobile/QgEtatDesLieux.tsx`

- [ ] **Step 1 : Mettre à jour la meta de la ligne Stockage**

Repérer le bloc dans le tableau `lignes` :

```tsx
{
  icon: Warehouse,
  titre: "Stockage",
  meta: `${stockTier.nom} · ${state.inventaireJoueur.length} obj.`,
  path: "/stockage",
},
```

Le remplacer par :

```tsx
{
  icon: Warehouse,
  titre: "Stockage",
  meta:
    stockTier.capaciteMax === Number.POSITIVE_INFINITY
      ? `${stockTier.nom} · ${state.inventaireJoueur.length} obj. · loyer ${stockTier.loyerHebdo} €/sem`
      : `${stockTier.nom} · ${state.inventaireJoueur.length}/${stockTier.capaciteMax} · loyer ${stockTier.loyerHebdo} €/sem`,
  path: "/stockage",
},
```

- [ ] **Step 2 : Vérifier le typecheck**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 3 : Commit**

```bash
git add src/components/mobile/QgEtatDesLieux.tsx
git commit -m "feat(qg): ligne Stockage affiche capacité et loyer hebdo"
```

---

### Task 3 : QG page — passer la prop météo + réordonner sections

**Files :**
- Modify : `src/app/qg/page.tsx`

- [ ] **Step 1 : Passer la prop météo conditionnelle à WeekTimeline**

Dans le StickyTop, remplacer :

```tsx
<div style={{ marginBottom: 10 }}>
  <WeekTimeline jourActuel={state.jourActuel} />
</div>
```

par :

```tsx
<div style={{ marginBottom: 10 }}>
  <WeekTimeline
    jourActuel={state.jourActuel}
    meteoSemaine={aGenBulletinMeteo(state) ? state.meteoSemaine : undefined}
  />
</div>
```

- [ ] **Step 2 : Réordonner les sections du body**

Dans le `<MobileLayout>`, après le bloc `dernierHuissier` conditionnel, l'ordre actuel est :

```
<GazetteTeaser ... />
<h2 ...>— État des lieux —</h2>
<QgEtatDesLieux ... />
<h2 ...>— Dernières sessions —</h2>
<QgHistorique ... />
```

Le remplacer par :

```tsx
<h2 style={sectTitle}>— État des lieux —</h2>
<QgEtatDesLieux state={state} />

<GazetteTeaser
  achetee={state.gazetteAchetee}
  jourActuel={state.jourActuel}
  tendances={state.tendances}
  categoriesConnues={categoriesConnuesTendance}
  meteo={meteo}
  revelerMeteo={aGenBulletinMeteo(state)}
  celebrite={state.celebriteActuelle}
  revelerCelebrite={aGenCarnetMondain(state)}
  onOuvrir={() => setGazetteOuverte(true)}
  onAcheter={() => acheterGazette()}
  budget={state.budget}
  prixGazette={PRIX_GAZETTE}
/>

<h2 style={sectTitle}>— Dernières sessions —</h2>
<QgHistorique state={state} />
```

(Le contenu de `GazetteTeaser` reste strictement identique, on déplace juste le bloc.)

- [ ] **Step 3 : Vérifier le typecheck**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 4 : Commit**

```bash
git add src/app/qg/page.tsx
git commit -m "feat(qg): météo sur WeekTimeline (gated) + remonter État des lieux"
```

---

### Task 4 : QG page — bouton Passer

**Files :**
- Modify : `src/app/qg/page.tsx`

- [ ] **Step 1 : Récupérer avancerJour depuis useGame**

Trouver le bloc `useGame()` (vers ligne 46-53) :

```tsx
const {
  state,
  isHydrated,
  acheterGazette,
  rerollMeteo,
  rerollCelebrite,
  marquerHuissierVu,
} = useGame();
```

Ajouter `avancerJour` :

```tsx
const {
  state,
  isHydrated,
  acheterGazette,
  rerollMeteo,
  rerollCelebrite,
  marquerHuissierVu,
  avancerJour,
} = useGame();
```

- [ ] **Step 2 : Étendre la grille des CTA à 3 colonnes et ajouter le bouton**

Dans le StickyTop, remplacer le `<div>` contenant les deux boutons :

```tsx
<div
  style={{
    display: "grid",
    gridTemplateColumns: "1.4fr 1fr",
    gap: 10,
  }}
>
  <button
    type="button"
    onClick={() => router.push("/chiner")}
    ...
  >
    Chiner
  </button>
  <button
    type="button"
    onClick={() => router.push("/vitrine")}
    ...
  >
    {state.vitrine ? "Reprendre l'étal" : "Exposer"}
  </button>
</div>
```

par (en gardant les styles inline existants pour Chiner et Exposer, et en ajoutant le 3ᵉ bouton) :

```tsx
<div
  style={{
    display: "grid",
    gridTemplateColumns: "1.2fr 1fr 0.8fr",
    gap: 10,
  }}
>
  <button
    type="button"
    onClick={() => router.push("/chiner")}
    style={{
      padding: "12px 8px",
      background: "var(--forest-800)",
      color: "var(--brass-300)",
      border: "1px solid var(--brass-500)",
      fontFamily: "var(--font-display)",
      fontSize: 12,
      letterSpacing: "0.16em",
      textTransform: "uppercase",
      boxShadow:
        "inset 0 0 0 2px var(--forest-800), inset 0 0 0 3px var(--brass-500)",
      cursor: "pointer",
    }}
  >
    Chiner
  </button>
  <button
    type="button"
    onClick={() => router.push("/vitrine")}
    style={{
      padding: "12px 8px",
      background: "var(--paper-100)",
      color: "var(--forest-800)",
      border: "1px solid var(--brass-500)",
      fontFamily: "var(--font-display)",
      fontSize: 12,
      letterSpacing: "0.16em",
      textTransform: "uppercase",
      boxShadow:
        "inset 0 0 0 2px var(--paper-100), inset 0 0 0 3px var(--brass-500)",
      cursor: "pointer",
    }}
  >
    {state.vitrine ? "Reprendre l'étal" : "Exposer"}
  </button>
  <button
    type="button"
    onClick={() => avancerJour(1)}
    style={{
      padding: "12px 8px",
      background: "var(--paper-200)",
      color: "var(--ink-700)",
      border: "1px solid var(--brass-500)",
      fontFamily: "var(--font-display)",
      fontSize: 12,
      letterSpacing: "0.16em",
      textTransform: "uppercase",
      boxShadow:
        "inset 0 0 0 2px var(--paper-200), inset 0 0 0 3px var(--brass-500)",
      cursor: "pointer",
    }}
  >
    Passer
  </button>
</div>
```

- [ ] **Step 3 : Vérifier le typecheck**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 4 : Commit**

```bash
git add src/app/qg/page.tsx
git commit -m "feat(qg): bouton Passer pour avancer d'un jour"
```

---

### Task 5 : Validation finale

**Files :** aucun

- [ ] **Step 1 : Typecheck global**

Run : `npx tsc --noEmit`
Expected : 0 erreur.

- [ ] **Step 2 : Build production**

Run : `npm run build`
Expected : build OK, pas d'erreur ni de warning bloquant.

- [ ] **Step 3 : Test visuel en dev**

Run : `npm run dev` puis ouvrir `http://localhost:3000/qg`.

Vérifier dans l'ordre :

1. Sans la compétence Bulletin météo (état initial d'une nouvelle partie) : la WeekTimeline n'affiche **pas** d'icônes météo, hauteur inchangée par rapport à avant.
2. Débloquer la compétence Bulletin météo (via console / nouvelle partie avec progression, ou directement modifier `state.competencesDebloquees`) : les 7 cellules de la timeline affichent maintenant une icône météo.
3. La section "État des lieux" apparaît au-dessus de la Gazette dans le scroll.
4. La ligne Stockage affiche `<tier> · <nb>/<max> · loyer <X> €/sem` (ou sans `/max` pour l'Entrepôt).
5. Le bouton "Passer" apparaît à droite, plus discret que Chiner/Exposer.
6. Cliquer "Passer" : le jour incrémente, la timeline met à jour le "aujourd'hui". Après 7 clics : le loyer est prélevé, les tendances se rafraîchissent, et un objet en restauration arrivant à terme est finalisé.
7. Cliquer "Passer" avec budget négatif après loyer : le bandeau Huissier s'affiche au prochain rendu.

- [ ] **Step 4 : Si tout est OK, signaler à l'utilisateur**

Le code est prêt pour merge / review.
